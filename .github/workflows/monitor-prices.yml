name: 检测并发布 App Store 订阅变动

on:
  schedule:
    - cron: "37 6,14,22 * * *"
      timezone: "Asia/Shanghai"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: appstore-price-data
  cancel-in-progress: false

jobs:
  detect-confirm-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: 读取当前数据
        uses: actions/checkout@v6

      - name: 使用 Node.js 22
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: 第一次抓取候选快照
        run: node scripts/fetch-price-snapshot.mjs --output .tmp/price-snapshot-first.json --fallback data/validation-snapshot.json

      - name: 校验第一次候选快照
        run: node scripts/validate-price-snapshot.mjs --snapshot .tmp/price-snapshot-first.json --previous data/validation-snapshot.json

      - name: 比较第一次订阅变动
        id: first-compare
        run: node scripts/compare-price-snapshots.mjs --before data/validation-snapshot.json --after .tmp/price-snapshot-first.json --json .tmp/price-diff-first.json --markdown .tmp/price-diff-first.md --github-output "$GITHUB_OUTPUT"

      - name: 写入第一次检测摘要
        run: |
          node scripts/render-update-report.mjs .tmp/price-snapshot-first.json >> "$GITHUB_STEP_SUMMARY"
          cat .tmp/price-diff-first.md >> "$GITHUB_STEP_SUMMARY"

      - name: 等待 Apple 页面稳定
        if: steps.first-compare.outputs.changed == 'true'
        run: sleep 480

      - name: 第二次抓取确认快照
        if: steps.first-compare.outputs.changed == 'true'
        run: node scripts/fetch-price-snapshot.mjs --output .tmp/price-snapshot-confirmed.json --fallback data/validation-snapshot.json

      - name: 校验第二次确认快照
        if: steps.first-compare.outputs.changed == 'true'
        run: node scripts/validate-price-snapshot.mjs --snapshot .tmp/price-snapshot-confirmed.json --previous data/validation-snapshot.json

      - name: 比较第二次订阅变动
        if: steps.first-compare.outputs.changed == 'true'
        id: second-compare
        run: node scripts/compare-price-snapshots.mjs --before data/validation-snapshot.json --after .tmp/price-snapshot-confirmed.json --json .tmp/price-diff-confirmed.json --markdown .tmp/price-diff-confirmed.md --github-output "$GITHUB_OUTPUT"

      - name: 确认两次变化完全一致
        if: steps.first-compare.outputs.changed == 'true'
        id: confirm
        run: node scripts/confirm-price-change.mjs --first .tmp/price-diff-first.json --second .tmp/price-diff-confirmed.json --github-output "$GITHUB_OUTPUT"

      - name: 准备已验证数据
        if: steps.first-compare.outputs.changed == 'false' || steps.confirm.outputs.confirmed == 'true'
        id: publish-data
        env:
          FIRST_CHANGED: ${{ steps.first-compare.outputs.changed }}
          CONFIRMED: ${{ steps.confirm.outputs.confirmed }}
        run: |
          if [ "$CONFIRMED" = "true" ]; then
            cp .tmp/price-snapshot-confirmed.json data/validation-snapshot.json
            node scripts/update-price-change-log.mjs --diff .tmp/price-diff-confirmed.json --log data/price-change-log.json
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            cp .tmp/price-snapshot-first.json data/validation-snapshot.json
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi
          node scripts/fetch-exchange-rates.mjs --output data/exchange-rates.json
          node scripts/write-deployment-marker.mjs
          echo "ready=true" >> "$GITHUB_OUTPUT"

      - name: 安装构建依赖
        if: steps.publish-data.outputs.ready == 'true'
        run: npm ci

      - name: 验证数据与静态网站
        if: steps.publish-data.outputs.ready == 'true'
        run: |
          npm run lint
          npm test

      - name: 写入确认结果摘要
        if: steps.confirm.outputs.confirmed == 'true'
        run: |
          printf '\n## 二次确认结果\n\n' >> "$GITHUB_STEP_SUMMARY"
          cat .tmp/price-diff-confirmed.md >> "$GITHUB_STEP_SUMMARY"

      - name: 提交已确认数据
        if: steps.publish-data.outputs.ready == 'true'
        id: commit
        run: |
          git diff --check
          git add data/validation-snapshot.json data/exchange-rates.json data/price-change-log.json public/deployment-status.json
          if git diff --cached --quiet; then
            echo "published=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Refresh verified App Store price snapshot"
          git push
          echo "published=true" >> "$GITHUB_OUTPUT"

      - name: 验证 Cloudflare 线上部署
        if: steps.commit.outputs.published == 'true'
        env:
          SITE_URL: ${{ vars.PUBLIC_SITE_URL || 'https://price.290935.xyz' }}
        run: node scripts/verify-public-deployment.mjs --url "$SITE_URL" --timeout-seconds 600

      - name: 发送 Bark 上线结果
        if: steps.commit.outputs.published == 'true' && steps.publish-data.outputs.changed == 'true' && success()
        continue-on-error: true
        env:
          BARK_PUSH_URL: ${{ secrets.BARK_PUSH_URL }}
          SITE_URL: ${{ vars.PUBLIC_SITE_URL || 'https://price.290935.xyz' }}
        run: node scripts/notify-bark.mjs --event published --summary .tmp/price-diff-confirmed.json --url "${SITE_URL%/}/changes/"

      - name: 保存检测与确认详情
        if: always() && steps.first-compare.outputs.changed == 'true'
        uses: actions/upload-artifact@v6
        with:
          name: price-confirmation-${{ steps.first-compare.outputs.fingerprint }}
          path: |
            .tmp/price-diff-first.md
            .tmp/price-diff-first.json
            .tmp/price-snapshot-first.json
            .tmp/price-diff-confirmed.md
            .tmp/price-diff-confirmed.json
            .tmp/price-snapshot-confirmed.json
          if-no-files-found: ignore
          retention-days: 7

      - name: 任务失败时发送 Bark
        if: failure()
        continue-on-error: true
        env:
          BARK_PUSH_URL: ${{ secrets.BARK_PUSH_URL }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: node scripts/notify-bark.mjs --event failure --url "$RUN_URL"
